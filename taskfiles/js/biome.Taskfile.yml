version: "3"

tasks:
  check:
    desc: Check format, lint, and imports with Biome
    deps: [":js:install-deps"]
    cmd: $JS_EXEC biome check

  "check:write":
    desc: Automatically fix format, lint, and imports with Biome
    deps: [":js:install-deps"]
    cmd: $JS_EXEC biome check --write
